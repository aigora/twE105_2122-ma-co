#pragma once
#include <stdio.h>
#include <stdbool.h>
#include <stdlib.h> //Preguntar si su uso est� permitido
#include <time.h> //Preguntar si su uso est� permitido
#include <SDL2/SDL.h>
#include <SDL2/SDL_timer.h>
#include <SDL2/SDL_image.h>
#include <SDL2/SDL_audio.h>
#include "Utilities.h"
#include "Tokens.h"

Tokens_struct* generarToken(SDL_Texture* Textura)
{
    int TP=30;
    Tokens_struct token[1];



    Token[0].texture.x = 300;
    Token[0].texture.y = 300;
    Token[0].texture.w = TP;
    Token[0].texture.h = TP;

    Textura->texture_player = Textura;


    return player;




}
